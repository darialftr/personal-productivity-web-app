import {AppState} from '../state.js';import {escape,subjectName} from '../ui.js';
let month=new Date().getMonth(),year=new Date().getFullYear();const names=['Ianuarie','Februarie','Martie','Aprilie','Mai','Iunie','Iulie','August','Septembrie','Octombrie','Noiembrie','Decembrie'];
export function changeMonth(delta){month+=delta;if(month<0){month=11;year--}if(month>11){month=0;year++}}
export function calendar(){const first=new Date(year,month,1),days=new Date(year,month+1,0).getDate(),offset=(first.getDay()+6)%7;let cells=Array(offset).fill('<div></div>');for(let day=1;day<=days;day++){const date=`${year}-${String(month+1).padStart(2,'0')}-${String(day).padStart(2,'0')}`,items=[...AppState.calendarEvents.filter(event=>event.event_date===date),...AppState.tasks.filter(task=>task.deadline_date===date)];cells.push(`<div class="calendar-day ${date===new Date().toISOString().slice(0,10)?'today':''}"><strong>${day}</strong>${items.slice(0,2).map(item=>`<span class="event-dot">${escape(item.title)}</span>`).join('')}</div>`)}return `<section class="card"><div class="section-head"><button class="secondary-button" data-calendar="-1">‹</button><div><p class="kicker">Planificare</p><h2>${names[month]} ${year}</h2></div><button class="secondary-button" data-calendar="1">›</button></div><div class="calendar-grid"><small>Lu</small><small>Ma</small><small>Mi</small><small>Jo</small><small>Vi</small><small>Sâ</small><small>Du</small>${cells.join('')}</div></section>`}
