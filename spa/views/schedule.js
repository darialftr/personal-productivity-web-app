import {AppState} from '../state.js';
export function schedule(){return `<section class="card"><p class="kicker">Săptămâna ta</p><h2>Orar</h2><p class="empty">Orarul existent folosește încă stocarea locală a aplicației vechi. Acest prototip nu îl dublează; migrarea lui va muta o singură sursă în AppState.</p></section>`}
